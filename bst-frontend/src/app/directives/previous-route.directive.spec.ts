import { PreviousRouteDirective } from './previous-route.directive';

describe('BackButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviousRouteDirective();
    expect(directive).toBeTruthy();
  });
});
